# تاریخچهٔ تغییرات — حساب‌وکتاب (Hesab)

تمام نگارش‌ها در یک فایل (`index.html`)، بدون مرحلهٔ ساخت (build-less) و بدون وابستگی زمان‌اجرا.

---

## v1.18.0 — کارت‌های کیف‌پولی حساب‌ها + پالت تأکیدی قابل انتخاب
- **حساب‌ها کارت شدند:** هر حساب با گرادیانِ ۱۴۰ درجه از رنگِ خودش رندر می‌شود (`acard is-wallet`) با چیپِ فلزی و نمادِ پرداختِ بدون‌تماس (SVG درون‌خطی با `currentColor`)، شمارهٔ کارتِ ماسک‌شده با ارقام فارسی (`•••• •••• •••• ۴۴۵۵`؛ ذخیرهٔ داده دست‌نخورده)، بجِ «منفی» برای ماندهٔ بدهکار، و هاورِ بالارونده. جوهرِ متن (سفید/تیره) و عمقِ گرادیان با `accSkin()` و حلقهٔ تطبیقی طوری انتخاب می‌شود که **هر دو سرِ گرادیان** حداقل ۴.۵:1 (WCAG AA) بماند — بدترینِ هشت رنگِ پیش‌فرض ۴.۶۱ (نسخهٔ اولِ تابع تا ۲.۵۱ می‌افتاد؛ در همین نسخه رفع شد).
- **پالت تأکیدی:** دکمهٔ `#accentToggle` در نوار بالا بین فیروزه (پیش‌فرض، بدون اتریبیوت) → لاجورد → انار → زعفران می‌چرخد؛ با کی‌فریم `accentSpin`، توستِ نام پالت و ماندگاری در `localStorage['hk-accent']` (بوتِ سربرگ پیش از اولین رندر اعمالش می‌کند). **فقط مقدارِ توکن‌ها عوض می‌شود** (`[data-accent]` + ترکیب با `[data-theme]`)، نه قواعدِ کامپوننت — لایهٔ اکسنت صفر سلکتور کامپوننتی دارد و نگهبانِ تستی‌اش سبز است. نامِ `--turq/--gold` عمداً عوض نشد (شکافِ رنگ‌اند، نه نامِ رنگ).
- **زیرساخت:** توابعِ رنگ (`hexRGB/rgbStr/mixHex/toBlack/toWhite/relLum/crat`)، `maskCard/toFaDigits/ACC_TYPE_FA`، و `initAccent()` با الگوی `initTheme()`. خطوطِ مویی و پس‌زمینه‌های نیمه‌شفافِ داخلِ کارت از `--whair/--wchip` می‌خوانند که JS بر اساس جوهر می‌دهد (کارتِ روشنِ جوهر-تیره هم خطِ مناسب خودش را دارد). `APP_VERSION=v1.18.0`، کشِ `sw.js` → `hesabketab-v22`.
- **تست‌ها:** بخش ۳۳ در `smoke.js` (+۲۰ بررسی، مجموع ۳۴۷) شامل نگهبانِ AA با محاسبهٔ مستقل در تست روی هر هشت `ACC_COLORS`؛ `session.js` بدون تغییر (۴۰ بررسی). هر دو سبز.
- جزئیات کامل (تغییرات کدِ شماره‌دار، باگ‌های رفع‌شده، «چه چیزی عوض نشد») در `CHANGELOG.md`.


- **چرا منو یک لحظه می‌رفت و برمی‌گشت؟** View Transition از پیش‌فرض از «کل صفحه» دو اسنپ‌شات می‌گیرد و آن‌ها را با هم محو می‌کند؛ منو (`.tabs`) هم داخل همان اسنپ‌شات بود، پس وسط دِروِل به‌طور موقت نیمه‌شفاف می‌شد.
- **راه‌حل:** `.tabs` گروه مستقلِ خودش را می‌گیرد (`view-transition-name:hnav`) و انیمیشن هر سه لایهٔ آن خاموش می‌شود؛ گروه‌های نام‌دار روی گروه `root` نقاشی می‌شوند، پس منو در تمام مدتِ انتقال یک‌دانه و کاملاً-opaque سر جایش می‌ماند.
- **خط زیرین:** `moveInd` از داخل `apply()` بیرون آمد و به `vt.finished` وصل شد — تا پیش از پایان محو، اسنپ‌شاتِ منو خط را در جای درست نگه می‌دارد و بعد از آن، ترنزیشن واقعیِ `.ind` آن را نرم سُر می‌دهد (قبلاً وسط انیمیشن می‌پرید).

## v1.17.0 — انتقال نرم بین صفحه‌ها
- **پرش ناگهانی چرا؟** دو حرکت روی هم می‌افتاد: اسنپ‌شاتِ View Transition از پنِ تازه در لحظه‌ای گرفته می‌شد که انیمیشن `fadeUp` خودش هنوز در `opacity:0` بود؛ نتیجه یک فلشِ خالی و بعد «پریدن» محتوا. همچنین اسکرول قبلی حفظ می‌شد، پس پنِ تازه در همان ارتفاعِ اسکرولِ صفحهٔ قبلی باز می‌شد.
- **راه‌حل:** در مرورگرهای دارای View Transition، انیمیشن خودِ پن خاموش می‌شود (`@supports (view-transition-name:none){.pane.active{animation:none}}`) و حرکت فقط دستِ View Transition است: خروجی فقط محو (`vtOut` بدون translate، ۱۸۰ms) و ورودی محو با ۶px بالا آمدن (۳۸۰ms با `--ease-out`).
- **اسکرول:** `setTab` پیش از عوض‌کردن پن‌ها `window.scrollTo(0,0)` می‌کند — داخل همان callback که صفحه فریز است، پس پرش دیده نمی‌شود.
- **مرورگرهای قدیمی‌تر:** `fadeUp .45s ease` با `paneIn .34s var(--ease-out)` (۸px) جایگزین شد.

## v1.16.0 — رفع باگ داک موبایل + بازگشت نوار کلاسیک دسکتاپ
- **باگ اصلی (از v1.14.0):** خط توکن‌های ناوبری داخل بلوک موبایل ناقص بود (`:root{--nav-h:64px};--nav-inset:…`) — یعنی `--nav-inset/--nav-side/--nav-max/--nav-total` هرگز تعریف نمی‌شدند. نتیجه: `bottom:calc(var(--nav-inset) + env(safe-area-inset-bottom))` نامعتبر می‌شد و داکِ `position:fixed` با `top:auto` به جای لبهٔ پایین، سر جای طبیعی‌اش در بالای صفحه می‌افتاد؛ توست، شیت «بیشتر» و padding بدنه هم همین سرنوشت را داشتند. حالا `:root` کامل و سالم است و همه‌چیز به لبهٔ پایین برمی‌گردد.
- **دسکتاپ:** بلوک `@media(min-width:1024px)` خالی‌شدنی شد؛ قواعد ریشه همان نوار متنی تمام‌عرض چسبان را می‌کشند (سطح `--surface-1`، خط زیرین گرادیانی، فقط متن). کی‌فریم بلااستفاده `dockDown` حذف شد.
- **همپوشانی‌ها در دسکتاپ:** شیت «بیشتر» و توست از زیر نوار بالا باز می‌شوند.

## v1.15.0 — دسکتاپ: منوی بالا به‌جای ستون کناری
- **ریل عمودی حذف شد:** بلوک `@media(min-width:1024px)` بازنویسی شد؛ `--rail-w` و `body{padding-inline-end}` بازنشسته شدند. دسکتاپ همان **منوی بالایی کلاسیک** را دارد که حالا مثل داک موبایل یک **پیل شیشه‌ای شناور** است: `position:sticky;top:var(--nav-inset)` با `--glass-bg/--glass/--e-4` و `border-radius:var(--r-pill)`؛ با اسکرول در ۱۰px بالای صفحه می‌چسبد. موبایل (`max-width:1023px`) ذره‌ای دست‌نخورد.
- **آیکن + برچسب:** برچسب‌های کوتاه هر بخش دوباره نمایان‌اند (`.tab-lbl{display:block}` با `--f-xs` زیر آیکن)؛ ترتیب هشت‌تایی دسکتاپ بدون تغییر: خانه، تراکنش، حساب، وام، دسته‌ها، بودجه، گزارش، تقویم. جستجوی سراسری در دسکتاپ همان چیپ بالای صفحه می‌ماند و `navSeq` هشت‌تایی سر جایش است.
- **قرص فعال افقی:** `.ind` دسکتاپ از نوار عمودی ۳px به همان قرص زیراسلات موبایل تبدیل شد (`--x/--w` + هالهٔ `color-mix`)؛ `moveInd` بدون هیچ تغییری هر دو حالت را تغذیه می‌کند.
- **ورود از بالا:** کی‌فریم‌های `dockDown` (افت فنری نوار) و `sheetDown` اضافه شدند؛ شیت «بیشتر» و توست‌ها در دسکتاپ از زیر نوار بالا باز می‌شوند (`top:calc(env(safe-area-inset-top) + var(--nav-total) + …)`).

## v1.14.0 — داک مدرن: جستجو جای وام، وام به «بیشتر»
- **چیدمان تازهٔ نوار پایین (موبایل):** خانه، تراکنش، حساب، **جستجو**، بیشتر. «جستجوی سراسری» (همان Ctrl+K با مدال gsModal) حالا یک اسلات دائمی داک است؛ چیپ جستجوی نوار بالا در موبایل کنار کشیده شد (`@media(max-width:1023px){.gsc-chip{display:none}}`) و دسکتاپ همان چیپ بالای صفحه را دارد (`.tab-search{display:none}` در ریشه؛ ریل هشت‌تایی دست‌نخورده).
- **وام‌ها به شیت «بیشتر» رفت:** `MORE_SECTIONS=['loans','cats','budget','reports','cal']` — پنج کاشی، وام سرِ فهرست. `tab-loans` در DOM سرِ جایش ماند (فقط `tab-opt` شد) تا ریل دسکتاپ ترتیبش را از دست ندهد. مورفینگ `#tabMore` عمومی بود و بدون تغییر کد، آیکن/برچسب وام را می‌پذیرد.
- **مدرن‌سازی حرکتی:** داک با `@keyframes dockIn` فنری از پایین می‌آید (`--ease-spring`)؛ ارتفاع `--nav-h` از ۶۲ به ۶۴px؛ سایه از `--e-3` به `--e-4` عمیق‌تر؛ قرص فعال (`.ind`) هالهٔ نرم با `color-mix(in srgb,var(--turq) 55%,transparent)` گرفت که در هر دو تم درست است؛ آیکن فعال با `translateY(-1px)` فنری بالا می‌پرد.
- **شیت «بیشتر» مثل برگهٔ واقعی:** `@keyframes sheetUp` (از پایین، جابه‌جای popInِ بالانشین)، دستگیرهٔ کشیدن (`.moresheet::before`، گرید-آیتم full-span)، چیپ گرد ۴۴px دور آیکن هر کاشی (`.moresheet button .ic`) و تینت فیروزه‌ای روی کاشی فعال (`.on`).
- **کیبورد:** `navSeq` حالا در موبایل «جستجو» را هم در ردیف می‌گمارد (`dash,tx,acc,search,more`) و Enter رویش `gsOpen()` را صدا می‌زند؛ در دسکتاپ فیلتر `tab-search` ترتیب هشت‌تایی را حفظ می‌کند. `setTab` برای `'search'` زودبرمی‌گردد (اسلات اکشن است، نه تب).
- **تست‌ها:** بخش ۲۸ اضافه شد (۱۷ ادعا: ورود فنری، sheetUp، دستگیره، چیپ آیکن، هالهٔ color-mix، لیفت آیکن، ۶۴px، reduced-motion، اسلات جستجو، پناه‌گیری چیپ بالا، پنج‌تایی شدن شیت) و ادعاهای ۲۱/۲۵ به واقعیت جدید به‌روز شدند (۹ اسلات، ۵ کاشی، ۵ tab-opt با ترتیب DOM واقعی `cats,loans,budget,reports,cal`، عمق `--e-4`). جلسه هم ۴۲ ادعا. مجموع سبز: **۳۱۵ + ۴۲**.

## v1.13.1 — دکمهٔ حساب بالاخره باز شد (باگ لایهٔ نامرئی)
- **مقصر اصلی:** توستِ اعلان («ذخیره شد ✓») با `opacity:0` پنهان می‌شد ولی هم `pointer-events` داشت هم جای‌گاه فیکس؛ روی موبایل دقیقاً وسطِ نوار پایین پارک می‌شد و **ضربه‌های اسلات‌های میانی (تراکنش، حساب، وام) را می‌بلعید** — خانه و بیشتر چون کنارش بودند سالم می‌ماندند. حالا `.toast` در حالت پنهان `visibility:hidden;pointer-events:none` است و در حالت نمایان هر دو برمی‌گردند. (توست فقط متن است؛ چیزی برای کلیک ندارد.)
- **باگ بوتِ مسیر بدون پین:** اگر پین تنظیم نشده باشد، IIFE فقط قفل را باز می‌کرد و دو صفحهٔ ورود/راه‌اندازی را **هرگز نمی‌بست**. حالا هر سه مسیر (نصب / پین / مستقیم) تکلیف هر سه سطح را روشن می‌کنند.
- **پاک‌سازی اشتباه خودم در v1.13:** قاعدهٔ `display:flex!important` که برای بوتِ CSS-first نوشته بودم داخل `@media print` نشسته بود (چون لنگر ویرایش، زیررشتهٔ خط چاپ بود!) — یعنی نه روی صفحه اثر داشت نه در پرینت درست بود؛ و اگر جابه‌جاش می‌کردم، `style.display='none'` جاوااسکریپت را برای همیشه می‌بست. حذف شد؛ صفحه‌ها با قاعدهٔ معمولی `display:flex` از همان اول دیده می‌شوند و JS حرف آخر را می‌زند.
- **اسکلت بوت واقعی شد:** قبلاً با `display:none` ذاتاً مخفی بود؛ حالا از لحظهٔ پارس CSS دیده می‌شود، `pointer-events:none` دارد و در همهٔ مسیرها با اولین `initApp()` از DOM بیرون می‌رود.
- **آزمون:** سه مسیر بوت (مستقیم/پین/نصب) + چرخهٔ قفل‌وباز + هر ۸ تب رفتاری سنجیده شد؛ ۴ ادعای جدید به smoke — **۲۹۸ + ۴۰، همه سبز**.

---

## v1.13.0 — بوتِ بدون نقطة مرده + کاشی‌های حساس به خودشان
- **پیش‌فرضِ درستِ بوت:** تا امروز همهٔ صفحه‌ها با `display:none!important` شروع می‌شدند و تا اجرای IIFEی آخر فایل، کاربر یک لحظهٔ خالی می‌دید (زمان پارس ۲۷۳KB + اجرا). حالا صفحهٔ ورود و راه‌اندازی با **CSS** پیش از جاوااسکریپت دیده می‌شوند و IIFE فقط «تصحیح» می‌کند: اگر دستگاه تازه باشد setup را نشان می‌دهد، اگر PIN نباشد مستقیم باز می‌کند. حالت‌های دیگر هر دو صفحه را می‌بندند.
- **اسکلت موج‌دار:** یک اسکلت سبک (خط + بلوک‌های نرم با انیمیشن `skWash`) تا لحظهٔ اولین `initApp()` همراهی می‌کند و همان‌جا از DOM حذف می‌شود؛ با `prefers-reduced-motion` بی‌حرکت است.
- **container query واقعی:** `.dash-bento` حالا `container-type:inline-size` دارد و زیر **۶۶۰pxِ خودش** (نه صفحه) تک‌ستون می‌شود و همهٔ کاشی‌ها تمام‌عرض می‌روند — پنجره‌های میانی ۶۶۰–۹۶۰px دیگر کاشی‌های له‌شده ندارند. مرورگرهای قدیمی همان رفتار media-query را می‌گیرند (پیش‌رونده).
- **بازرسی صادقانهٔ پالت JS:** تنها canvas برنامه کارت‌ماهِ خروجی است و پالت ثابتِ برند دارد — توکنیزه‌کردنش خروجی تم روشن را خراب می‌کرد، پس **عمداً دست نخورد**. بقیهٔ hexها دادهٔ پالت دسته‌ها در localStorage هستند و اصلاً رنگِ رابط نیستند.
- **آزمون:** بخش ۲۷ با ۱۱ ادعا — مجموعاً **۲۹۴ + ۴۰، همه سبز**.

---

## v1.12.0 — کنتراست و راست‌به‌چپِ تمیز
- **`--dim` به حد AA رسید:** روی تیره‌ترین سطح (`--surface-3`، جایی که toast و کارت‌های شیشه‌ای می‌نشینند) از **۴.۰۷ به ۴.۶۶** و در تم روشن از **۴.۲۷ به ۴.۶۶**. رنگ‌ها با جست‌وجوی کامل فضای RGB انتخاب شدند تا **نزدیک‌ترین** رنگ به اصلی باشند که هر شش ترکیب (دو رنگ متن × سه سطح) را به ≥۴.۵ برساند — تاریک `#84a8a2 → #88b6a4`، روشن `#5d7d78 → #587678`. سلسله‌مراتب حفظ شد: `--dim` همچنان ضعیف‌تر از `--muted` است.
- **سه نشانگر لبه به خواص منطقی رفتند:** `modal-x`، `tpl-del` و `bell-badge` از `left` فیزیکی به `inset-inline-end` رسیدند. در این چیدمان RTL نتیجهٔ بصری عین قبل است، اما حالا معنا «سمتِ پایان» است نه «چپ» — اگر روزی `dir="ltr"` شود درست می‌ماند.
- **بازرسی کامل RTL انجام شد:** هر ۱۴ قاعدهٔ دارای `left/right` فیزیکی بررسی شد. نتیجه: هیچ باگ بصری وجود ندارد — `.ind` و `.ut-glider` عمداً فیزیکی‌اند چون JS با `offsetLeft` فیزیکی می‌سنجد و تبدیل‌شان به منطقی می‌شکست؛ `lcard::before/after` و g1/g2 هر دو طرف را می‌پوشانند؛ toast و modal-x؟  مرکز/جای درست.
- **آزمون:** بخش ۲۶ با ۱۶ ادعا اضافه شد که **کنتراست را از توکن‌های CSS خود فایل محاسبه می‌کند** (نه عدد ثابت) — اگر کسی بعداً رنگی را عوض کند، تست خودش شکست می‌خورد. مجموعاً **۲۸۳ + ۴۰ ادعا، همه سبز**.

---

## v1.11.0 — جای بودجه و وام عوض شد + رفع بلعیده‌شدن لمس
- **گروه‌بندی تازه:** پایین صفحه حالا `خانه · تراکنش · حساب · وام · بیشتر` است و بودجه به شیشهٔ «بیشتر» رفت (کنار دسته‌ها، گزارش‌ها و تقویم).
- **باگ کلیک میانی‌ها:** قرصِ نشانگر المانی `position:absolute` بود که **بالاتر از تب‌ها** نقاشی می‌شد (تب‌ها `position:relative` نداشتند) و چون `pointer-events:none` هم نداشت، لمس‌هایی که روی آن ناحیه می‌افتادند را می‌بلعید. اسلات آخر سالم بود چون `.tab-more` اشتباهاً `position:relative` داشت — دقیقاً همان دلیلی که «اولی و آخری درست، میانی‌ها بد» به‌نظر می‌رسید.
- **رفع:** `.ind{pointer-events:none}` (یک عنصر تزئینی نباید هیچ کلیکی بگیرد) + `position:relative` روی `.tab` تا خودِ تب‌ها هم بالای قرص باشند.
- **آزمون:** دو ادعای رگرسیون برای همین دو نکته + به‌روزرسانی سه ادعای گروه‌بندی — مجموعاً **۲۶۷ + ۴۰ ادعا، همه سبز**.

---

## v1.10.0 — چهار آیتم مهم، بقیه در «بیشتر»
- **از هشت به پنج:** داک شناور فقط خانه، تراکنش، حساب و بودجه را نگه داشت + یک اسلات «بیشتر». هر اسلات روی گوشی ۳۶۰px حالا ۷۲px جا دارد، نه ۴۵px.
- **شیشهٔ «بیشتر»:** دسته‌ها، وام‌ها، گزارش‌ها و تقویم داخل یک شیت شناورِ دو در دو باز می‌شوند، درست بالای داک و با همان زبان شیشه‌ای (همان `--glass-bg` و `--glass`؛ دستور پخت تازه‌ای اختراع نشد). کاشی‌ها ۷۶px ارتفاع دارند تا با شست راحت باشند.
- **داک همیشه می‌گوید کجای کاری:** وقتی یکی از آن چهار بخش باز باشد، خودِ اسلات «بیشتر» شکل همان بخش را می‌گیرد — آیکون و نامش عوض می‌شود و نشانگر قرصی زیرش می‌لغزد. بدون این، انتخاب «گزارش‌ها» داک را بی‌آیتمِ فعال می‌گذاشت و خراب به‌نظر می‌رسید.
- **پیمایش کیبورد درست شد:** ترتیب کلیدهای جهت حالا از **وضعیت** می‌آید (`navSeq()`)، نه از اندازه‌گیری چیدمان. نسخهٔ اول با `offsetParent` نوشته شد و هم در محیط تست می‌شکست هم شکننده بود؛ آیتم‌های پنهان از گردش بیرون می‌مانند و «بیشتر» با کلید جهت باز می‌شود.
- **ARIA تمیز ماند:** «بیشتر» `role=tab` ندارد — یک `aria-haspopup="dialog"` است، پس tablist هنوز فقط هشت تب واقعی دارد.
- **دسکتاپ فرقی نکرد:** ریل عمودی همچنان هر هشت بخش را یک‌جا نشان می‌دهد؛ آنجا جای کافی هست و مشکل شلوغی فقط در موبایل بود.
- **چرا بقیه به نوار بالا برنگشتند:** دو نوار ناوبری در دو طرف صفحه یعنی کاربر برای پیدا کردن یک بخش باید دو جا را بگردد، و نوار بالا جای چیپ‌های وضعیت است. «بیشتر» همان کار را با یک لمس اضافه و بدون تقسیم توجه انجام می‌دهد.
- **آزمون:** بخش ۲۵ با ۲۹ ادعا (ساختار، شیت، ریخت‌تغییری اسلات، `navSeq` در دو حالت، بستن با کلیک بیرون و resize) — مجموعاً **۲۶۵ + ۴۰ ادعا، همه سبز**.

---

## v1.9.0 — داک شناور شیشه‌ای
- **از لبه کناره گرفت:** نوار منو دیگر به پایینِ صفحه نچسب نیست؛ ۱۰px از پایین و ۱۲px از دو طرف فاصله دارد و مثل داک iOS وسط می‌نشیند. روی تبلت عریضِ بی‌قواره نمی‌شود — تا ۴۶۰px سقف دارد.
- **شیشهٔ مات:** `backdrop-filter` روی همان توکن `--glass` که اسکریم مودال‌ها داشت (عدد بلورِ تازه اختراع نشد). سطح نیمه‌شفاف هم توکن تازهٔ `--glass-bg` است که در قالب روشن هم عوض می‌شود — پس سوییچ تم همچنان فقط تعویض توکن است.
- **لبهٔ شیشه‌ای:** یک هایلایت موئی روی لبهٔ بالا (`inset 0 1px 0 var(--sheen)`) + سایهٔ عمق `--e-3` زیرش. `overflow:hidden` باعث می‌شود قرصِ نشانگر و پس‌زمینهٔ آیتم‌ها داخل گوشهٔ گرد بریده شوند.
- **بازخورد لمس:** با انگشت، آیتم به ۹۲٪ کوچک می‌شود و سریع (۹۰ms) می‌نشیند؛ برگشتش با سرعت عادی است — همان حس فشرده‌شدن iOS.
- **fallback:** مرورگرهایی که `backdrop-filter` ندارند (WebView قدیمی) سطح توپر می‌گیرند، نه شیشهٔ شکسته.
- **لایه‌های رویی:** توست و پنل اعلان به `--nav-total` (ارتفاع + فاصله + safe-area) منتقل شدند تا زیر داک نروند.
- **دسکتاپ دست‌نخورده:** ریل عمودی توپر می‌ماند؛ شیشه فقط جایی است که لایه واقعاً روی محتوا شناور است.
- **آزمون:** بخش ۲۴ با ۱۶ ادعا + به‌روزرسانی ۶ ادعای بازنشستهٔ بخش ۲۳ — مجموعاً **۲۳۶ ادعا، همه سبز**.

---

## v1.8.0 — منوی اصلی پایین صفحه
- **نوار ناوبری پایین:** در عرض <۱۰۲۴px تب‌بار از بالای صفحه به پایین آمد — همان‌جا که شست می‌رسد. نوار بالا فقط برند و چیپ‌ها را نگه داشت.
- **الگوی Material 3 navigation bar:** هشت منو در یک ردیف جا می‌شوند چون **فقط آیتم فعال برچسب نشان می‌دهد** و بقیه فقط آیکون‌اند. برچسب‌ها کوتاه شدند (خانه، تراکنش، حساب، دسته، وام، بودجه، گزارش، تقویم)؛ نام کامل در `aria-label` و در tooltipِ ریل دسکتاپ دست‌نخورده ماند.
- **نشانگر قرصیِ لغزان:** خطِ زیرِ تب به یک قرص رنگی تبدیل شد که زیر آیتم فعال می‌لغزد. مرکز شدنش با `calc(var(--x) + 3px)` و `width:calc(var(--w) - 6px)` انجام می‌شود — یعنی **هیچ محاسبه‌ای در JS لازم نشد** و همان `moveInd()` هر سه حالت (خط بالا، قرص پایین، نوار لبهٔ ریل) را تغذیه می‌کند.
- **safe-area:** ارتفاع نوار با `--nav-h` و `env(safe-area-inset-bottom)` تنظیم می‌شود؛ `body` به اندازهٔ همان‌قدر پایین فضا می‌گیرد.
- **بالا آمدن لایه‌های رویی:** توست، پنل اعلان و شیت‌های پایینی حالا بالای نوار می‌نشینند، نه زیرش.
- **ساده‌سازی:** متغیر `--tabs-h` که برای جبران ارتفاع نوار بالا ساخته شده بود کاملاً حذف شد — چون دیگر نوار چسبانی بالا نیست. `.dayhead` و کارت فرم به مقدار ثابت برگشتند و یک گوشهٔ JS هم پاک شد.
- **دسکتاپ دست‌نخورده:** ریل عمودی آیکون‌دار سر جایش است.
- **آزمون:** بخش ۲۳ با ۲۰ ادعا (چیدمان پایین، قرص، برچسب کوتاه/نام کامل، بالا آمدن توست و پنل، نبودِ `--tabs-h`) — مجموعاً **۲۱۶ ادعا، همه سبز**.

---

## v1.7.0 — ایده از Novo Admin (mini-sidebar)
- **ریل آیکون دسکتاپ:** در عرض ≥۱۰۲۴px نوار تب افقی به یک ریل عمودی ۷۲ پیکسلی در سمت راست تبدیل می‌شود؛ هر بخش آیکون SVG خودش را گرفت و متن تب به‌عنوان **tooltip** روی هاور/فوکوس زنده می‌ماند (پس چیزی از دست نمی‌رود). نشانگر رنگی به‌جای خط زیرِ تب، روی لبهٔ ریل عمودی می‌لغزد — با همان `--x/--y/--w/--h` و بدون سبک‌خوانی layout.
- **منوی ⋯ روی کارت‌ها:** یک پاپ‌اور مشترک + دفترچهٔ کارها. هفت کارت (حساب‌ها، دسته‌ها، وام‌ها، دوره‌ای‌ها، بودجهٔ داشبورد، آخرین تراکنش‌ها، حلقهٔ بودجه) گزینه‌های خودشان را گرفتند: افزودن، پرش، اجرای فوری و **خروجی CSV از همان داده**. با `role=menu`، پیمایش با کلیدهای بالا/پایین و Home/End، Esc، و بازگشت فوکوس.
- **پنل یادآوری:** زنگوله به‌جای پرش مستقیم، پنلی باز می‌کند که سررسیدها را در سه گروه «عقب‌افتاده / امروز / ۷ روز آینده» نشان می‌دهد و «نمایش همهٔ اقساط» دارد. منبع داده با بنر داشبورد یکی شد (`dueItems()`) — دیگر دو جا منطق تکراری نیست. در موبایل به شیت پایینی تبدیل می‌شود.
- **حلقهٔ KPI:** کارت «نرخ پس‌انداز» از نوار خطی به حلقهٔ `conic-gradient` تبدیل شد با رنگ معنایی (سبز >۵۰٪، طلایی >۲۰٪، مرجانی زیر آن). `--p` با `@property` ثبت شد تا واقعاً انیمیت شود.
- **اسپارک‌لاین روی کارت‌های درآمد و هزینه:** نمودار ۳۰ روزهٔ جداگانه برای هر کدام. `spark()` حالا اندازه می‌گیرد و **شناسهٔ گرادیان یکتا** می‌سازد — با سه نمودار هم‌زمان، هر سه رنگ خودشان را داشتند و همه به اولی فیکس می‌شدند.
- **نوارهای میکرو:** زیر هر تراکنش در داشبورد نوار باریک «اندازهٔ نسبی مبلغ» (با `scaleX`، نه `width`) و در ردیف دوره‌ای‌ها نوار پیشرفت دورهٔ جاری.
- **بالا آمدن کارت با هاور:** روی دستگاه‌های نشانگر-ظریف (`@media (hover:hover) and (pointer:fine)`) — تا روی موبال هاور چسبنده ندهد.
- **رفع باگ واقعی:** کلیک برنامه‌ایِ لینک دانلودِ پشتیبانِ خودکار تا `document` حباب می‌شد و **همان لحظه هر منوی بازِ کاربر را می‌بست**. حالا `stopPropagation` دارد و مسیر بستنِ بیرونی، کلیکِ لینک‌های دانلود را نادیده می‌گیرد.
- **آزمون:** بخش ۲۲ با ۳۴ ادعا + گسترش `tests/session.js` (کلیک واقعی روی منوها، منوی ⋯، پنل اعلان، حلقه و اسپارک‌لاین) — مجموعاً **۱۹۶ + ۴۰ ادعا، همه سبز**.

---

## v1.6.0 — بازطراحی مدرن (سطح ۰ تا ۲)
- **رفع باگ: کلیک روی منوها کار نمی‌کرد** — خط اتصال کلیک تب‌ها در بازآرایی `setTab` اشتباهاً حذف شده بود (پیمایش با کیبورد و پیوندهای «مدیریت ←» سالم بودند و همین، باگ را از آزمون جا انداخته بود). برگشت و برایش ادعای رگرسیون نوشته شد.
- **حذف چیپ ساعت** از نوار بالا؛ فقط تاریخ می‌ماند. `tickClock()` دیگر متن ساعت نمی‌نویسد و بررسی تغییر روز از هر ثانیه به هر ۳۰ ثانیه رسید (کارهای تغییر روز — سررسیدها، اعلان، تراکنش دوره‌ای — دست‌نخورده).
- **مستحکم‌سازی `alpha()`:** رنگ نامعتبر یا غایب دیگر کل صفحه را خالی نمی‌کند؛ هنگام بارگذاری، رنگِ حساب و دستهٔ بی‌رنگ بازسازی می‌شود.
- **آزمون نشست واقعی:** `tests/session.js` با دادهٔ ذخیره‌شده بوت می‌کند و هر ۸ منو را واقعاً کلیک می‌کند (۱۹ ادعا).
- **معماری توکن:** یک لایهٔ توکن کامل اضافه شد — ۵ شعاع، ۷ فاصله، ۸ اندازهٔ قلم، ۴ سطح ارتفاع، ۴ مدت و ۴ منحنی حرکت. پوشش توکن‌ها از ۳۸۴ به بیش از ۷۰۰ استفادهٔ `var()` رسید و ۲۹۱ مقدار خام جایگزین شد.
- **تم روشن = تعویض توکن:** ۲۸ قاعدهٔ patch روی `[data-theme="light"]` حذف شد و به ۷ قاعدهٔ واقعی کاهش یافت؛ کامپوننت‌ها فقط توکن می‌خوانند.
- **کنتراست (WCAG AA):** `--muted` و `--dim` در تم تیره بالا آمد (۳٫۰۹ → ۴٫۷۵ روی پنل)؛ در تم روشن فیروزه‌ای، طلایی، مرجانی، سبز و `--dim` به بالای ۴٫۵:۱ تیره شدند. رنگ‌های برند (شش هگز اصلی) دست‌نخورده ماندند.
- **سطح‌های تونال به‌جای خط‌کشی:** کارت‌ها حاشیه ندارند؛ عمق با پلهٔ رنگی `--surface-1..4` + سایهٔ دولایه (تماس + محیطی) و یک هایلایت داخلی ساخته می‌شود.
- **تایپوگرافی پول:** مبالغ از Lalezar (بدون رقم هم‌عرض) به Vazirmatn 700 + `font-variant-numeric: tabular-nums` منتقل شدند؛ ارقام ستونی دقیق زیر هم می‌نشینند. Lalezar فقط برای عنوان‌ها ماند. **ارقام فارسی سرِ جایشان ماند.**
- **قلم متغیر:** Vazirmatn با `wght@100..900` در یک درخواست بارگذاری می‌شود (به‌جای شش وزن ثابت).
- **راست‌به‌چپ:** ستون مبالغ `text-align:end`؛ `letter-spacing` روی متن فارسی حذف شد؛ درخشش نئونی روی عدد اصلی برداشته شد.
- **حرکت:** همهٔ `transition:.2s` (که ضمناً `all` بود) به یک توکن مشترک با فهرست صریح ویژگی‌ها تبدیل شد؛ نشانگر تب و کشوی واحد نمایش از `left/width` به `transform` منتقل شدند؛ منحنی‌ها استاندارد Material 3 شد؛ `prefers-reduced-motion` از «خام‌کردن همه‌چیز» به «کاهش هدفمند» اصلاح شد و `countUp` و اسکرول نرم هم از آن پیروی می‌کنند.
- **حذف حلقه‌های بی‌پایان:** درخشش دکمه، جاروی لوگو، برق نوارها، هالهٔ «امروز» و شن‌ذره‌های متحرک حذف شد؛ تنها نبضِ «وضعیت زندهٔ پشتیبان‌گیری» باقی ماند.
- **شیشه فقط روی لایهٔ overlay:** `backdrop-filter` از نوار بالا و نوار تب برداشته شد (هزینهٔ گرافیکی روی اندروید میان‌رده) و فقط روی scrim مودال ماند.
- **شیت پایینی:** در موبایل (≤۶۴۰px) فرم‌ها به‌صورت bottom sheet با دستهٔ کشیدن و `safe-area` باز می‌شوند؛ `100dvh` جای `100vh`.
- **داشبورد Bento:** شبکهٔ ۶ ستونه با اندازه‌های نامتقارن (کارت دارایی ۴ ستون، سه کارت کوچک ۲ ستون، بودجه و آخرین تراکنش‌ها ۳ ستون).
- **رشتهٔ «نسبت به ماه قبل»:** زیر موجودی، تغییر خالص ماه جاری نسبت به ماه گذشته با ▲/▼ و رنگ معنایی (تکیه‌به‌رنگ نیست).
- **دسترس‌پذیری:** `role=tablist/tab/tabpanel` + `aria-selected` + پیمایش با کلیدهای جهت و Home/End؛ `role=dialog aria-modal` روی هر ۱۰ مودال؛ `aria-label` روی دکمه‌های آیکون‌دار؛ `content-visibility` و `contain:paint` برای فهرست‌های بلند.
- **اندازهٔ هدف لمسی:** دکمه‌های کوچک (ویرایش، حذف، بستن، رنگ، پیمایش تقویم) با `::before` به ۴۴px رسیدند بدون تغییر ظاهر؛ `min-height:44px` روی بخش‌بندی‌ها.
- **آیکون SVG درون‌خطی:** اسپرایت ۱۰ نمادی بدون وابستگی اضافه شد؛ ایموجی‌های نوار ابزار (جستجو، اعلان، بستن، واردات، قالب) با SVG جایگزین شدند. ایموجی برای دسته‌ها (که کاربر ویرایش می‌کند) عامدانه ماند.
- **چاپ و PWA:** `print-color-adjust:exact` + `break-inside:avoid`؛ `meta theme-color` با تم فعال همگام می‌شود؛ کَش سرویس‌ورکر به `hesabketab-v8`.
- **حذف چیپ «ذخیرهٔ خودکار» و چیپ ساعت** از نوار بالای صفحه — ذخیره‌سازی بدون تغییر است و فقط نشانگر بصری حذف شد.
- **آزمون:** بخش ۲۱ به `tests/smoke.js` اضافه شد (۴۶ ادعای جدید روی توکن‌ها، کنتراست، ساختار Bento، ARIA، حرکت و حذف چیپ) — مجموعاً **۱۵۵ ادعا، همه سبز**.

---

## v1.5.0 — واردات از اکسل و CSV
- **خوانندهٔ `.xlsx` بدون کتابخانه** مستقیم داخل برنامه اضافه شد: فایل xlsx در واقع یک ZIP است؛ با پیمایش central directory و بازکردن `deflate-raw` با `DecompressionStream` و خواندن XML با `DOMParser`. هیچ داده‌ای آپلود نمی‌شود و کاملاً آفلاین کار می‌کند.
- **پارسر CSV/TSV** با پشتیبانی از نقل‌قول‌ها و تشخیص خودکار جداکننده (`,` `;` `tab`) + **چسباندن مستقیم از کلیپ‌بورد** (کپی از اکسل/گوگل‌شیت).
- **ویرایشگر سه‌مرحله‌ای** (`#impModal`): منبع ← ستون‌ها ← پیش‌نمایش. انتخاب شیت (شیتِ پر خودکار انتخاب می‌شود)، انتخاب ردیف سرستون، و تعیین **نقش** هر ستون.
- **تشخیص خودکار نقش ستون‌ها** از روی سرستون فارسی/انگلیسی: تاریخ، شرح، مبلغ، برداشت/واریز (ستون‌های جداگانهٔ صورت‌حساب بانکی)، نوع، واحد، شخص، دسته، حساب، یادداشت. ستون‌های «جمع کل»/«ردیف»/«مانده» خودکار نادیده گرفته می‌شوند.
- **نرمال‌سازی تاریخ:** شمسی متنی (`1404/03/12`)، میلادی (`2026-05-23`)، و **شماره‌تاریخ اکسل** (`45435`) — با کلید تبدیل خودِ برنامه (`J`).
- **واحد و مبلغ:** ریال/تومان از انتخاب کاربر یا ستون «واحد» هر ردیف؛ جداکنندهٔ هزارگان، منفی انتهای عدد و پرانتز مالی پشتیبانی می‌شود. واحد پایهٔ برنامه تومان است (ریال ÷ ۱۰).
- **تفکیک خودکار دسته** از روی کلیدواژهٔ شرح (ساختمان، قبض، خوراک، حمل‌ونقل، سلامت، پوشاک، آموزش، تفریح، مسکن) روی دسته‌های موجودِ برنامه.
- **ایمنی:** ردیف تکراری (همان تاریخ + مبلغ + شرح) خودکار رد می‌شود؛ واردات **اضافه می‌کند، جایگزین نمی‌شود**؛ پیش‌نمایش تعداد آماده/تکراری/نامعتبر را نشان می‌دهد؛ هر دستهٔ وارداتی با `imp:<batchId>` برچسب می‌خورد و دکمهٔ **«بازگردانی آخرین واردات»** آن را کامل حذف می‌کند.
- `state.imports` اضافه شد (در مقداردهی/ترمیم وضعیت و در بازیابی JSON پاک می‌شود چون داده‌ها جایگزین می‌گردند).
- دکمهٔ «📥 واردات اکسل» در نوار پشتیبان‌گیری فوتر · `Esc` مودال را می‌بندد · در `@media print` مستثنی شد.
- دودتست: بخش ۲۰ با **۳۱ بررسی جدید** — شامل ساخت یک فایل xlsx واقعی درون تست (ZIP + `deflateRawSync`) برای آزمودن مسیر unzip/sharedStrings/serial-date، و آزمون رفت‌وبرگشت واردات/بازگردانی. مجموع بررسی‌ها: **۱۰۹**.
- صحت‌سنجی با فایل واقعی کاربر (۵ شیت، ۴۴۸ ردیف): ۱۸۵ ردیف با جمع **۱۸٬۶۵۶٬۳۸۷٬۶۱۲ ریال** — دقیقاً برابر «جمع کل» خود شیت.
- `sw.js`: کش `hesabketab-v7`.

## v1.4.2 — حذف نام از نوار بالا
- نام کاربر (`#brandName`) از زیر لوگو در هدر حذف شد — نوار بالا فقط شامل عنوان «حساب‌وکتاب» و امضای گرافیکی است.
- **ویرایش نام به فوتر منتقل شد:** دکمهٔ «👤 نام من» در ردیف اکشن‌های فوتر، مودال نام را باز می‌کند و نام فعلی را در tooltip نشان می‌دهد.
- نام همچنان در **کارت ماهانه** و **سربرگ گزارش چاپی** استفاده می‌شود؛ متن مودال خوش‌آمدگویی به همین موضوع اصلاح شد.
- امضای طراح در پایین صفحه («طراحی و توسعه توسط مهدی عسکری») بدون تغییر باقی می‌ماند.
- دودتست: بخش ۱۹ (۷ بررسی جدید) + نسخه‌محور شدن بخش ۱۸ — مجموع ۷۸ بررسی.
- `sw.js`: کش `hesabketab-v6`.

## v1.4.1 — مودال «چه چیزهایی جدید است؟»
- بعد از هر به‌روزرسانی، بار اولی که برنامه باز می‌شود مودال ✨ «چه چیزهایی جدید است؟» نمایش داده می‌شود:
  - فهرست امکانات تازهٔ هر نسخه + **تاریخ انتشار شمسی**
  - نسخهٔ فعلی با نشان «جدید» و پس‌زمینهٔ متمایز
- فقط یک بار در هر نسخه نشان داده می‌شود (ذخیره در `localStorage: hk-whatsnew-seen`)؛ بستن با دکمه، کلیک بیرون یا Esc.
- دودتست: بخش ۱۸ (۱۲ بررسی جدید) — مجموع ۶۹ بررسی.
- `sw.js`: کش `hesabketab-v5`.

## v1.4.0 — جستجوی سراسری
- **پالت جستجو با Ctrl+K / ⌘K** یا دکمهٔ 🔍 در نوار بالا؛ یک ورودی، همهٔ داده‌ها:
  - تراکنش‌ها (توضیح، دسته، شخص، روش، حساب، مبلغ — اعداد فارسی/عربی هم)
  - حساب‌ها (نام، شماره کارت) · وام‌ها (نام، بابت) · دوره‌ای‌ها · دسته‌ها · اشخاص
- ناوبری کیبورد: **↑↓ حرکت، Enter باز کردن، Esc بستن**؛ بدون نتیجه → حالت خالی با راهنما.
- **پرش هوشمند:** انتخاب نتیجه به تب مربوطه می‌رود و همان آیتم را با انیمیشن `gs-flash` اسکرول و هایلایت می‌کند.
- هایلایت عبارت منطبق با `<mark>` + نرمال‌سازی فارسی (`gsNorm`: ی/ك عربی، أ/إ/آ، ة).
- دودتست: بخش ۱۷ (۱۱ بررسی جدید) — مجموع ۵۷ بررسی.
- `sw.js`: کش `hesabketab-v4`.

## v1.3.0 — تراکنش‌های دوره‌ای، کارت ماه، گزارش چاپی
- **تراکنش‌های دوره‌ای (Recurring):** تعریف قلم‌های تکرارشونده با تکرار ماهانه / هفتگی / هر N روز (+ روز سررسید و گام تعداد روز).
  - ثبت **خودکار** سررسید‌های جاافتاده هنگام باز کردن برنامه (محدود به ۱۳ دوره در هر بازکرد تا از حلقه جلوگیری شود).
  - حالت **یادآوری** (ثبت دستی) با کلید خودکار/دستی برای هر قلم.
  - کارت «دوره‌ای‌ها» در ستون تراکنش‌ها + بنر سررسیدهای امروز (تراکنش و دوره‌ای).
- **کارت ماهانهٔ اشتراکی:** تصویر ۱۰۸۰×۱۹۲۰ (Canvas) با تراز ماه، نرخ پس‌انداز و پرهزینه‌ترین دسته‌ها؛ دکمهٔ ذخیره و اشتراک سیستمی (Web Share API).
- **گزارش چاپی / PDF:** خروجی A4 بدون کتابخانه (از چاپگر خودِ مرورگر) با `@media print`؛ دکمهٔ چاپ در گزارش‌ها.
- `sw.js`: نسخهٔ کش `'hesabketab-v3'` و استراتژی network-first برای سند اصلی (همیشه آخرین نسخه پس از انتشار).
- پشتیبان‌گیری برنامه اکنون فیلد `recurring` را هم شامل می‌شود (بازیابی بدون خطا).

## v1.2.1 — بهبودها و رفع اشکال
- فراردهی (escape) نام دسته‌بندی در بودجه و بخش‌های گزارش (جلوگیری از شکستن نما با ورودی‌های HTML).
- هش کردن پاسخ سوال امنیتی (ذخیرهٔ ایمن به‌جای متن ساده) + مهاجرت رمزهای قبلی.
- دقت ریال/تومان: حذف گرد کردن در تبدیل ورودی؛ خروجی CSV با گرد کردن صحیح و نقل‌قول خودکار فیلدها.
- جستجوی حساس‌به‌حالت (case-insensitive)؛ پنل روز کارمزدِ متعلق‌به را پنهان نمی‌کند.
- انتخاب‌گرهای حساب پس از ذخیره به‌روز می‌شوند؛ حساب جدید خودکار انتخاب می‌شود.
- نشانگر تب پس از بارگذاری فونت‌ها؛ رندر نیمه‌شب برای سررسیدهای جدید؛ بازگرداندن پاک‌سازی (sanitize) هنگام بازیابی.
- رفع خطای `matchMedia` در مرورگرهای بدون پشتیبانی؛ فعال‌سازی زوم صفحه (حذف `user-scalable=no`).
- **ویژگی جدید:** اتصال به فایل ثابت پشتیبان با File System Access API («connect to fixed file»).

## پیش از v1.2.1
نگارش‌های اولیه شامل هستهٔ برنامه: داشبورد، تراکنش‌ها، حساب‌ها، وام و اقساط، بودجه، گزارش‌ها، تقویم شمسی، قفل پین و پشتیبان‌گیری خودکار.
