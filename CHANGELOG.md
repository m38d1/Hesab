# CHANGELOG — حساب‌وکتاب (Hesab)

قالب ثبت بر اساس الگوی `01_Projects/Projeyar/CHANGELOG.md`: هر نسخه یک بخش کامل
(تغییرات کاربری، تغییرات کد شماره‌دار، باگ‌های رفع‌شده، تست‌ها، و «چه چیزی عوض نشد»).

---

## v1.18.0 — ۱۹ شهریور ۱۴۰۵ — کارت‌های کیف‌پولیِ حساب‌ها + پالت تأکیدی قابل انتخاب

**اسنپ‌شات:** `backup/index-20260910-110014.html` · بر پایه v1.17.1 · خطوط `index.html`: ۲۲۹۹ → ۲۴۲۶
**ایده‌ها:** از پروژهٔ `poolhamko` (کارت‌های رنگی شبیه کارت بانکی + پالت قابل انتخاب) — پیاده‌سازی دوباره با توکن‌ها و استاندارد کنتراست خودِ حساب‌وکتاب، بدون کپیِ CSS یا وابستگیِ CDN.

### الف) تغییرات کاربری

۱. **حساب‌ها دیگر بلوک‌های خاکستریِ یکسان نیستند؛ کارت‌اند.** هر حساب با گرادیانِ
   رنگِ خودش (همان رنگی که در ویرایش حساب انتخاب شده) drawn می‌شود: چیپِ فلزی و
   نمادِ پرداختِ بدون‌تماس، نام و نوع حساب، ماندهٔ درشت روی کارت، و پاورقیِ
   «شمارِ تراکنش‌ها». کارت‌ها در هاور بالا می‌آیند و سایه‌شان پرتر می‌شود.
۲. **شمارهٔ کارت ماسک می‌شود.** به‌جای نمایشِ ۱۶ رقم کامل، فقط ۴ رقمِ آخر با
   ارقامِ فارسی می‌ماند و بقیه با «•••• » پر می‌شود: `•••• •••• •••• ۴۴۵۵`.
   شماره‌های ۴ رقمی یا کوتاه‌تر بدونِ ماسک و فقط با ارقام فارسی نمایش داده می‌شوند.
   (ذخیرهٔ داده دست‌نخورده است؛ فقط نمایش عوض شده.)
۳. **ماندهٔ منفی بج می‌گیرد.** روی کارتِ حسابِ بدهکار، در کنارِ عددِ مانده،
   برچسبِ «منفی» می‌نشیند تا منفی‌بودنِ عدد با رنگ تنها تفهیم نشود.
۴. **پالت تأکیدی قابل انتخاب شد.** دکمهٔ تازه‌ای (دو دایرهٔ رنگی کنارِ هم) در نوار
   بالای صفحه، کنارِ «تغییر قالب» آمد. هر کلیک پالت را می‌چرخاند:
   **فیروزه** (پیش‌فرض) → **لاجورد** 🔷 → **انار** 🍎 → **زعفران** 🧡 → دوباره فیروزه.
   با هر تغییر، توستِ کوتاه نامِ پالت را می‌گوید و انتخاب در `localStorage['hk-accent']`
   مانده می‌شود؛ در بازدیدِ بعدی، پالت **پیش ازِ اولین رندر** اعمال می‌شود (بدونِ چشمک).
   پالت روی رنگِ تأکیدی، رنگِ طلایی، سطح‌ها (ساحه‌های پس‌زمینه)، خطوطِ مویی،
   حاشیهٔ فوکوس، هالهٔ دکمه‌ها و نورهای تزئینیِ پشتِ صفحه اثر می‌گذارد.
۵. کنتراستِ متنِ روی کارت‌ها خودکار انتخاب می‌شود: روی رنگ‌های روشنِ حساب،
   جوهرِ تیره و روی رنگ‌های تیره، جوهرِ سفید — به‌شرطی که در **هر دو سرِ** گرادیان
   حداقلِ ۴.۵:۱ (WCAG AA) حفظ شود.

### ب) تغییرات کد (شماره‌دار)

۱. **CSS کارتِ کیف‌پولی** — بلوک تازه پس از `.acard-foot` (~۱۸ خط):
   `.acard.is-wallet` (پوستِ کارت) + `.acard-mid/.acard-chip/.acard-wave/.acard-no/.neg-pill`.
   خطوطِ مویی و پس‌زمینه‌های نیمه‌شفافِ داخلِ کارت از دو متغیرِ تازهٔ کارت می‌خوانند:
   `--whair` و `--wchip` (که JS مقدارشان را بر اساس جوهرِ انتخابی می‌دهد). هیچ
   قاعدهٔ تازه‌ای برای کارت‌هایِ بدونِ `is-wallet` نوشته نشد.
۲. **لایهٔ پالت** — داخلِ `<style id="themeCSS">`، پس از بلوکِ نور:
   `[data-accent="lajevard|anar|zafaran"]` برایِ مقادیرِ مشترک،
   `[data-theme="dark"][data-accent=…]` برایِ سطح‌ها/متنِ تیره،
   `[data-theme="light"][data-accent=…]` برایِ حالتِ روشن (مقادیرِ تیره‌ترِ AA- safe).
   **فقط «مقدارِ» توکن عوض می‌شود؛ هیچ سلکتورِ کامپوننتی در این لایه نیست**
   (تستِ نگهبان: «accent layer adds no component rules»).
   فیروزه هیچ قاعده‌ای ندارد — همان مقادیر `:root` است.
   نامِ توکن‌ها (`--turq` و `--gold`) عمداً عوض **نشد**: اینها «شکافِ رنگ»اند نه نامِ رنگ؛
   `var(--turq)` ۸۱ بار در فایل به کار رفته و بازنویسیِ نام‌ها ریسکِ بی‌مورد داشت.
   دو قاعدهٔ `[data-theme="light"] .g1/.g2` که رنگِ فیروزه/طلایی را هک‌شده داشتند
   (`rgba(12,143,126,.15)` / `rgba(176,118,26,.12)`) به `var(--turq-soft)` و
   `var(--gold-soft)` تبدیل شدند تا نورهای تزئینی هم با پالت بیایند.
۳. **دکمهٔ پالت** — `.accent-toggle` در `#themeCSS` (الگو از `.theme-toggle`: همان
   ۴۲px، همان `--line2/--ink`) + کی‌فریمِ `accentSpin` (۵۵۰ms، `--ease-spring`) +
   `.accent-toggle.spin`؛ و در `@media (prefers-reduced-motion:reduce)` خاموش شد.
   در HTML: یک `<button id="accentToggle">` با دو `<span id="atA">/<span id="atB">`
   بلافاصله پس از `#themeToggle` در `.top-chips`.
۴. **اسکریپت بوت** (بلاک `<script>` انتهای `<head>`): علاوه بر `data-theme`،
   `localStorage['hk-accent']` را هم می‌خواند و اگر `lajevard/anar/zafaran` بود
   `data-accent` را روی `<html>` می‌گذارد (پیش ازِ ساختنِ DOM → بدونِ FOUC).
   مقدارِ نامعتبر یا نبودِ مقدار = بی‌تأثیر (فیروزه، یعنی مقادیر `:root`).
۵. **توابعِ کمکیِ تازه** کنارِ `alpha` در بلاکِ منطق: `hexRGB`، `rgbStr`، `mixHex`،
   `toBlack`، `toWhite`، `relLum` (نورِ نسبیِ WCAG)، `crat` (نسبتِ کنتراست)،
   `accSkin(hex)` → `{hi,lo,fg,hair,chip}`، `toFaDigits`، `maskCard`، `ACC_TYPE_FA`
   (برچسبِ فارسیِ نوعِ حساب: نقدی/کارت/کیف پول/پس‌انداز).
   `accSkin` یک‌بارِ تابعِ هدف است: اگر کنتراستِ جوهرِ تیره روی رنگِ خام ≥۴.۶ باشد،
   همان جوهر می‌ماند و سرِ تیرهٔ گرادیان تا **عمیق‌ترین حدِ مجازِ AA** تیره می‌شود
   (جوهرِ تیره با تیره‌ترشدنِ پس‌زمینه بدتر می‌شود، پس سقف اینجا بستگی است)؛
   اگر نه، جوهرِ سفید انتخاب و کلِ کارت به اندازهٔ لازم تیره می‌شود
   (جوهرِ سفید با تیره‌ترشدن بهتر می‌شود، پس کفِ AA روی سرِ روشنِ گرادیان بسته می‌شود).
   `lumOf` (آستانهٔ نورِ ساده) در همین نشست حذف شد — جواب نمی‌داد (بخش «پ» بند ۱).
۶. **`renderAccounts`** بازنویسی شد: کلاسِ `acard is-wallet`، استایلِ درون‌خطیِ
   `background:linear-gradient(140deg,hi,lo);color:fg;--whair:…;--wchip:…`،
   دو `<svg>` تزئینیِ `acard-chip/acard-wave` با `stroke="currentColor"`,
   `sub` = شمارهٔ ماسک‌شده یا برچسبِ نوع، و بجِ «منفی» برایِ `bal<0`.
   قلاب‌هایِ رویداد دست‌نخورده ماندند: `data-acc`، `[data-aedit]`، `[data-adel]`،
   کلاسِ `cardno` (که در جستجوی سراسری هم استفاده می‌شود).
۷. **موتورِ پالت**: `ACCENTS` (چهار پالت با دو رنگِ نشان + نامِ فارسی + آیکن)،
   `ACCENT_ORDER`، و `initAccent()` با الگویِ `initTheme()` (خواند/نوشت از
   `localStorage['hk-accent']`، چرخش با کلیک، `paint()` که `data-accent` و رنگِ
   دو دایرهٔ دکمه را ست می‌کند، توستِ «پالت … فعال شد»). فراخوانیِ `initAccent()`
   در `initApp()` درست بعدِ `initTheme()`.
۸. **نسخه**: `APP_VERSION` → `v1.18.0`، آیتمِ تازهٔ `WHATS_NEW[0]` (تاریخِ ۱۹ شهریور
   ۱۴۰۵)، و کامنتِ سربرگِ فایل که از `v1.5.0` جا مانده بود هم به‌روز شد.
۹. **`sw.js`**: نامِ کش `hesabketab-v21` → `hesabketab-v22` تا کاربرِ آفلاین
   پس ازِ اولین بازگشت، نسخهٔ تازه را بگیرد.

### پ) باگ‌ها و اشکال‌هایی که **همین نشست** پیدا و رفع کرد

۱. **کنتراستِ کارت‌ها زیرِ AA بود.** نسخهٔ اولِ `accSkin` دو ایراد داشت: جوهر را با
   «کدام نزدیک‌تر است» انتخاب می‌کرد و بعد سرِ تیرهٔ گرادیان را `+0.22` سیاه‌تر می‌کرد؛
   دقیقاً همان‌جا که جوهرِ تیره در بدترین حالت است. نتیجه: رویِ کارتِ طلایی
   (`#e9b44c`) کنتراست به **۲.۵۱:۱** می‌افتاد (برایِ متنِ ۱۱.۵px فاجعه). هیچ‌کدام از
   هشت رنگِ پیش‌فرض هم با آن گرادیان به ۴.۵ نمی‌رسید (بهترین ۳.۹۳).
   رفع: انتخابِ جوهر بر پایهٔ «شدنِ کار» (اگر جوهرِ تیره روی رنگِ خام AA نیست،
   حتماً سفید + تیره‌کردنِ کلِ کارت) و بستمند‌کردنِ **هر دو سرِ** گرادیان روی ۴.۶.
   امروز بدترین حالت در هشت رنگ: **۴.۶۱** (سرِ تیرهٔ `#e9b44c`).
۲. **بجِ «منفی» روی کارت‌های تیره ناپدید می‌شد** (پس‌زمینهٔ `rgba(0,0,0,.32)` روی
   پس‌زمینهٔ تیره). رفع: بج حالا دورخطِ `currentColor` و پس‌زمینهٔ `--wchip` دارد،
   یعنی با جوهرِ همان کارت یکی می‌شود.
۳. **نگهبانِ «قالب روشن فقط سواپِ توکن است»** در تست‌ها می‌تاخت، چون هر occurrence از
   `[data-theme="light"]` را می‌شمرد و بلوک‌هایِ تازهٔ پالت (`[data-theme="light"][data-accent=…]`)
   هم توکن‌اند نه وصله. رفع: معیار دقیق شد — فقط قواعدِ **فرزند** (وصلهٔ کامپوننت)
   شمرده می‌شوند؛ آستانه همان ≤۸ ماند و تعدادِ واقعی الان ۶ است.
   (یعنی نگهبان از قبل ضعیف بود، نه اینکه این نسخه چیزی را خراب کرده باشد.)
۴. تاریخِ `WHATS_NEW` اول `۱۸ شهریور` نوشته شد؛ با تبدیلِ دقیقِ امروز به شمسی
   (`Intl … u-ca-persian`) به `۱۹ شهریور ۱۴۰۵` اصلاح شد.
۵. آف‌بای‌وانِ تستِ خودم: سه کلیک از فیروزه = زعفران، نه انار. تست به چرخشِ کاملِ
   چهارپله (لاجورد → انار → زعفران → فیروزه) تبدیل شد تا دیگر قابل‌خطا نباشد.

### د) تست‌ها

- `tests/smoke.js`: بخشِ **۳۳** تازه («v1.18 wallet cards + accent palettes») با
  **۲۰ بررسی**: وجودِ پوستِ `is-wallet` و توابع، نبودِ کارت پیش ازِ داده، رندرِ دو کارت،
  چیپ/موج، **ماسکِ شماره با ارقام فارسی** (`•••• •••• •••• ۴۴۵۵`، بدونِ `6037`)،
  گرادیانِ درون‌خطی، بجِ منفی، پیش‌فرضِ فیروزه، چرخشِ کاملِ پالت‌ها و ماندنِ آن در
  `localStorage`، «لاجورد فقط مقدارِ توکن را عوض می‌کند»، «هر پالت سطح‌های تیره را
  رنگ می‌کند»، «لایهٔ پالت قاعدهٔ کامپوننت اضافه نمی‌کند»، و
  **نگهبانِ AA**: کنتراستِ جوهر با **هر دو سرِ** گرادیان برای **هر هشتِ** `ACC_COLORS`،
  محاسبه‌شده در تست با پیاده‌سازیِ **مستقل** (نه با توابعِ خودِ برنامه).
  جمعِ بررسی‌ها: **۳۴۷** (پیش‌تر ۳۲۷).
- به‌روزرسانیِ اجباریِ نسخه در تست‌ها: ۶ جایگاهِ `APP_VERSION==='v1.18.0'`،
  `WHATS_NEW[0].v`، و `/hesabketab-v22/` در تستِ سرویس‌ورکر.
- `tests/session.js`: بدونِ تغییر (۴۰ بررسی) — به `data-accent` یا ساختارِ کارت وابسته نیست.
- نتیجهٔ هر دو سوئیت: **✅ ALL TESTS PASSED** / **✅ REAL SESSION OK**
  (`NODE_PATH=<jsdom> node tests/smoke.js` و `node tests/session.js`).

### هـ) این نسخه چه چیزی را عوض نکرد

- **اسکیمای داده** و کلیدِ ذخیره (`hk-finance-v2`): هیچ فیلدِ تازه‌ای اضافه، حذف یا
  تغییرنام نیافت؛ شمارهٔ کارت همان‌طور که ذخیره می‌شد (لاتین، با فاصله) می‌ماند و
  **فقط در نمایش** ماسک می‌شود. پشتیبان‌گیری/بازیابی/وارداتِ اکسل و CSV دست‌نخورده‌اند.
- **هیچ وابستگی، CDN یا کتابخانه‌ای اضافه نشد**؛ خروجی همان یک فایل است.
- **PIN، سؤال امنیتی، هش‌کردن، قفلِ خودکار** و کلِ مسیرِ احراز هویت تغییر نکرد.
- منطقِ مالی (مانده، تبدیلِ تومان/ریال، بودجه، وام و اقساط، دوره‌ای‌ها، گزارش‌ها،
  تقویم، جستجوی سراسری، کارتِ ماهانه) تغییری نکرد؛ فقط `renderAccounts` در نمایش عوض شد.
- نامِ توکن‌های رنگ عوض نشد (فقط مقدار) — برای همین هیچ کامپوننتی لازم به ویرایش نداشت.
- چیدمان، ناوبری، انیمیشنِ انتقالِ صفحه، داک/نوار بالا، فونت‌ها و اندازه‌ها ثابت ماند؛
  به `#accGrid` چیزی اضافه نشد (همان گریدِ `minmax(260px,1fr)`).
- تست‌های موجود حذف یا شل نشدند؛ تنها دو سنجه به‌روز شد: شمارشِ نسخه/کش، و معیارِ
  نگهبانِ «وصلهٔ نور» که در بند «پ-۳» توضیح آمده.
